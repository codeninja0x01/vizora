---
phase: 08-bulk-generation
plan: 04
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - editor/src/app/(protected)/dashboard/bulk-generate/page.tsx
  - editor/src/app/(protected)/dashboard/bulk-generate/csv-uploader.tsx
  - editor/src/app/(protected)/dashboard/bulk-generate/field-mapper.tsx
  - editor/src/app/(protected)/dashboard/bulk-generate/preview-table.tsx
  - editor/src/app/(protected)/dashboard/bulk-generate/actions.ts
  - editor/src/app/(protected)/dashboard-sidebar.tsx
  - editor/src/app/(protected)/dashboard/templates/template-card.tsx
autonomous: true

must_haves:
  truths:
    - "User can navigate to Bulk Generate page from sidebar and from template cards"
    - "User can upload a CSV file and see parsed columns auto-mapped to template merge fields"
    - "User can manually override auto-mapped field assignments with dropdown selectors"
    - "User can preview all rows with per-row validation status before submitting"
    - "User can submit valid rows to batch API and receive confirmation with batch ID"
  artifacts:
    - path: "editor/src/app/(protected)/dashboard/bulk-generate/page.tsx"
      provides: "Multi-step bulk generation wizard page"
      min_lines: 50
    - path: "editor/src/app/(protected)/dashboard/bulk-generate/csv-uploader.tsx"
      provides: "CSV file upload with PapaParse client-side parsing"
      min_lines: 40
    - path: "editor/src/app/(protected)/dashboard/bulk-generate/field-mapper.tsx"
      provides: "Column-to-field mapping UI with auto-match suggestions"
      min_lines: 60
    - path: "editor/src/app/(protected)/dashboard/bulk-generate/preview-table.tsx"
      provides: "Validation preview table with per-row status"
      min_lines: 50
    - path: "editor/src/app/(protected)/dashboard/bulk-generate/actions.ts"
      provides: "Server actions for template fetching and batch submission"
      exports: ["getTemplatesForBulk", "submitBatch"]
  key_links:
    - from: "editor/src/app/(protected)/dashboard/bulk-generate/csv-uploader.tsx"
      to: "papaparse"
      via: "Papa.parse for client-side CSV parsing"
      pattern: "import.*Papa.*from.*papaparse"
    - from: "editor/src/app/(protected)/dashboard/bulk-generate/field-mapper.tsx"
      to: "editor/src/lib/batch/field-matcher.ts"
      via: "autoMapFields for initial column suggestions"
      pattern: "autoMapFields"
    - from: "editor/src/app/(protected)/dashboard/bulk-generate/actions.ts"
      to: "editor/src/lib/batch/queue.ts"
      via: "queueBatchRenders for batch submission"
      pattern: "queueBatchRenders"
---

<objective>
Build the dashboard Bulk Generate page with CSV upload, column-to-field mapping, validation preview, and batch submission.

Purpose: Implements BULK-02 (CSV upload and mapping). This is the primary dashboard workflow where users upload a CSV, map columns to template merge fields with auto-suggestions, preview and validate all rows, then submit the batch. Per user decision, CSV upload is dashboard-only (API is JSON-only).

Output: Multi-step wizard page accessible from sidebar navigation and template card shortcuts.
</objective>

<execution_context>
@/home/solo/.claude/get-shit-done/workflows/execute-plan.md
@/home/solo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-bulk-generation/08-RESEARCH.md
@.planning/phases/08-bulk-generation/08-CONTEXT.md
@.planning/phases/08-bulk-generation/08-01-SUMMARY.md
@.planning/phases/08-bulk-generation/08-02-SUMMARY.md

@editor/src/app/(protected)/dashboard-sidebar.tsx
@editor/src/app/(protected)/dashboard/templates/template-card.tsx
@editor/src/app/(protected)/dashboard/templates/actions.ts
@editor/src/lib/batch/types.ts
@editor/src/lib/batch/field-matcher.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bulk generate page with CSV uploader and field mapper</name>
  <files>
    editor/src/app/(protected)/dashboard/bulk-generate/page.tsx
    editor/src/app/(protected)/dashboard/bulk-generate/csv-uploader.tsx
    editor/src/app/(protected)/dashboard/bulk-generate/field-mapper.tsx
    editor/src/app/(protected)/dashboard/bulk-generate/actions.ts
  </files>
  <action>
**Create `editor/src/app/(protected)/dashboard/bulk-generate/actions.ts`** - server actions:
- `getTemplatesForBulk()`: Fetch user's templates with merge fields (session-scoped, returns id, name, mergeFields, mergeSchema for dropdown selector)
- `submitBatch(templateId: string, mergeDataArray: Record<string, unknown>[])`: Server action that creates Batch + Render records and queues via queueBatchRenders. Returns `{ batchId, totalCount }` on success. This wraps the same logic as the API endpoint but for dashboard use (session auth instead of API key).

**Create `editor/src/app/(protected)/dashboard/bulk-generate/page.tsx`** - multi-step wizard:
- 'use client' component with step state: `'select-template' | 'upload-csv' | 'map-fields' | 'preview' | 'submitted'`
- Read `?templateId=xxx` from URL search params to pre-select template (per user decision: shortcut from template pages)
- Step 1 (select-template): Dropdown/combobox to pick a template. Load templates via getTemplatesForBulk server action on mount. If templateId in URL, auto-select and advance to step 2.
- Step 2 (upload-csv): Render CSVUploader component. On successful parse, advance to step 3 with parsed headers and rows.
- Step 3 (map-fields): Render FieldMapper component with CSV headers and template merge fields. On confirm, advance to step 4 with mapped data.
- Step 4 (preview): Render PreviewTable component. On submit, call submitBatch and advance to step 5.
- Step 5 (submitted): Show success message with batch ID and link to /dashboard/renders.
- Navigation: "Back" button on each step to go to previous step. Step indicator showing progress (1/4 or progress dots).
- Page header with animation matching other dashboard pages:
  ```tsx
  <h1 className="text-2xl font-bold tracking-tight font-heading">Bulk Generate</h1>
  <p className="mt-1 text-muted-foreground">Generate videos in bulk from CSV data.</p>
  ```

**Create `editor/src/app/(protected)/dashboard/bulk-generate/csv-uploader.tsx`**:
- 'use client' component accepting `onParsed(headers: string[], rows: Record<string, unknown>[])` callback
- Dropzone area with dashed border, Upload icon, "Drop CSV file here or click to browse" text
- Accept only `.csv` files via input accept attribute
- On file select or drop:
  - Parse with PapaParse client-side: `Papa.parse(file, { header: true, dynamicTyping: true, skipEmptyLines: 'greedy', transformHeader: (h) => h.replace(/^\uFEFF/, '').trim() })`
  - Per research pitfall #1: strip BOM from first header for Excel compatibility
  - On complete: extract headers from meta.fields, pass headers + data array to onParsed callback
  - On error: show error toast via sonner
- Show file info after upload (filename, row count, column count)
- "Remove" button to clear and re-upload

**Create `editor/src/app/(protected)/dashboard/bulk-generate/field-mapper.tsx`**:
- 'use client' component accepting `csvHeaders: string[]`, `templateFields: MergeField[]`, `onConfirm(mapping: Record<string, string>)` callback
- On mount, call autoMapFields (import from batch/field-matcher - note: this is a client component, so import the function directly since string-similarity is a client-safe library)
- Display a table/list where each row shows:
  - CSV column name (left)
  - Arrow icon (center)
  - Template field dropdown (right) showing all available template fields
  - Confidence badge if auto-matched (green if > 0.7, yellow if > 0.4)
- Pre-select auto-matched fields in dropdowns
- Include "Unmapped" option in dropdown for columns the user wants to skip
- Warning message if any required template fields are not mapped
- "Confirm Mapping" button enabled when at least one field is mapped
- The mapping output is a Record<string, string> of csvColumn -> templateFieldKey
  </action>
  <verify>
1. TypeScript compilation: `cd editor && npx tsc --noEmit --skipLibCheck` passes
2. page.tsx implements multi-step wizard with 5 steps
3. csv-uploader.tsx uses PapaParse with BOM stripping
4. field-mapper.tsx calls autoMapFields and pre-selects auto-matched fields
5. actions.ts exports getTemplatesForBulk and submitBatch
6. URL param ?templateId pre-selects template
  </verify>
  <done>
Bulk generate page implements multi-step wizard: template selection (with URL param pre-select), CSV upload with PapaParse client-side parsing and BOM handling, field mapper with auto-match suggestions and manual override dropdowns. Server actions handle template listing and batch submission.
  </done>
</task>

<task type="auto">
  <name>Task 2: Preview table, submit flow, and navigation updates</name>
  <files>
    editor/src/app/(protected)/dashboard/bulk-generate/preview-table.tsx
    editor/src/app/(protected)/dashboard-sidebar.tsx
    editor/src/app/(protected)/dashboard/templates/template-card.tsx
  </files>
  <action>
**Create `editor/src/app/(protected)/dashboard/bulk-generate/preview-table.tsx`**:
- 'use client' component accepting:
  - `rows: Record<string, unknown>[]` (parsed CSV data)
  - `mapping: Record<string, string>` (csvColumn -> templateFieldKey)
  - `mergeFields: MergeField[]` (template field definitions for validation)
  - `onSubmit(validRows: Record<string, unknown>[])` callback
  - `isSubmitting: boolean`

- On mount/when data changes, validate each row:
  - Apply the column mapping to transform CSV row data to merge data format
  - For each row, validate against the template's merge field definitions
  - Track validation results: `{ rowIndex, valid, errors, mappedData }`

- Per user decision: flag-and-continue strategy (not block entire upload):
  - Show table with columns: Row #, Status (checkmark/X icon), mapped field values, Error details
  - Valid rows show green checkmark, invalid rows show red X with error tooltip
  - Summary bar at top: "X of Y rows valid" with visual indicator

- Row toggle: checkbox to include/exclude rows from submission (invalid rows unchecked by default, user can manually uncheck valid rows too)

- "Submit Batch" button at bottom:
  - Disabled when no rows selected or isSubmitting
  - Shows count: "Submit X renders"
  - On click, filters to selected valid rows, transforms to merge data format using mapping, calls onSubmit
  - Loading state with spinner during submission

- Per user decision: "Preview/dry-run required before rendering -- show table of all rows with mapped data and validation status, user confirms before any renders start"

**Update `editor/src/app/(protected)/dashboard-sidebar.tsx`**:
- Import `FileSpreadsheet` from lucide-react (or `TableProperties` - use an icon that conveys CSV/bulk operations)
- Add "Bulk Generate" to the mainNav array after "Renders":
  ```typescript
  { href: '/dashboard/bulk-generate', icon: FileSpreadsheet, label: 'Bulk Generate' },
  ```

**Update `editor/src/app/(protected)/dashboard/templates/template-card.tsx`**:
- Import `FileSpreadsheet` from lucide-react
- Add a "Bulk Generate" action button in the card's action area (alongside the existing delete button):
  - Small icon button or text link
  - Wrap in a `<Link>` to `/dashboard/bulk-generate?templateId=${template.id}`
  - Stop propagation to prevent the parent card link from firing: wrap in `onClick={(e) => e.preventDefault()}` div (same pattern as delete button)
  - Tooltip or title: "Bulk Generate"
  - Per user decision: "Accessible from both a dedicated 'Bulk Generate' dashboard page AND as a shortcut action from template detail pages"
  </action>
  <verify>
1. TypeScript compilation: `cd editor && npx tsc --noEmit --skipLibCheck` passes
2. preview-table.tsx shows validation status per row (green check / red X)
3. preview-table.tsx has row toggle checkboxes and "Submit X renders" button
4. dashboard-sidebar.tsx has "Bulk Generate" nav item
5. template-card.tsx has "Bulk Generate" action button linking to /dashboard/bulk-generate?templateId=xxx
6. Invalid rows unchecked by default but not blocked (flag-and-continue per discretion)
  </verify>
  <done>
Preview table shows all rows with per-row validation status, row toggle checkboxes (invalid unchecked by default), summary bar, and "Submit X renders" button. Sidebar nav includes "Bulk Generate" link. Template cards have "Bulk Generate" shortcut button linking to bulk generate page with pre-selected template.
  </done>
</task>

</tasks>

<verification>
1. All files compile: `cd editor && npx tsc --noEmit --skipLibCheck`
2. Navigate to /dashboard/bulk-generate shows multi-step wizard
3. CSV upload parses file client-side with PapaParse
4. Field mapper auto-matches columns with confidence indicators
5. Preview table shows validation status per row
6. Submit sends valid rows to batch API
7. Sidebar has Bulk Generate link
8. Template cards have Bulk Generate shortcut
</verification>

<success_criteria>
- Multi-step wizard: Select Template -> Upload CSV -> Map Fields -> Preview -> Submitted
- PapaParse client-side parsing with BOM stripping and dynamic typing
- Auto-match with Dice coefficient > 0.4, manual override via dropdowns
- Preview table with per-row validation, row toggle checkboxes, summary bar
- Flag-and-continue: invalid rows flagged but not blocked
- Submit calls server action that creates Batch + Renders and queues
- Navigation from sidebar and template card shortcuts both work
</success_criteria>

<output>
After completion, create `.planning/phases/08-bulk-generation/08-04-SUMMARY.md`
</output>
