---
phase: 11-ai-features
plan: 06
type: execute
wave: 2
depends_on: ["11-05"]
files_modified:
  - editor/src/components/editor/ai/storyboard-wizard.tsx
  - editor/src/components/editor/ai/scene-editor.tsx
  - editor/src/components/editor/ai/style-picker.tsx
  - editor/src/stores/storyboard-store.ts
  - editor/src/app/(protected)/dashboard/text-to-video/page.tsx
autonomous: true
must_haves:
  truths:
    - "User can describe video scenes in a storyboard interface with per-scene descriptions and durations"
    - "User can select a style/mood preset that guides the visual output"
    - "User can add, remove, and reorder scenes in the storyboard"
    - "Generated composition opens in the editor as an editable project"
    - "User can preview stock footage thumbnails before generating"
  artifacts:
    - path: "editor/src/components/editor/ai/storyboard-wizard.tsx"
      provides: "Multi-step storyboard wizard with scene input, style selection, and generation"
      exports: ["StoryboardWizard"]
    - path: "editor/src/components/editor/ai/scene-editor.tsx"
      provides: "Individual scene editing card with description, duration, text overlay"
      exports: ["SceneEditor"]
    - path: "editor/src/components/editor/ai/style-picker.tsx"
      provides: "Video style/mood preset picker grid"
      exports: ["StylePicker"]
    - path: "editor/src/stores/storyboard-store.ts"
      provides: "Zustand store for storyboard state management"
      exports: ["useStoryboardStore"]
    - path: "editor/src/app/(protected)/dashboard/text-to-video/page.tsx"
      provides: "Dashboard page entry point for text-to-video"
  key_links:
    - from: "editor/src/components/editor/ai/storyboard-wizard.tsx"
      to: "/api/ai/text-to-video"
      via: "fetch POST to generate composition from scenes"
      pattern: "fetch.*api/ai/text-to-video"
    - from: "editor/src/components/editor/ai/storyboard-wizard.tsx"
      to: "openvideo"
      via: "jsonToClip converts composition elements to Studio clips"
      pattern: "jsonToClip|studio\\.addClip"
    - from: "editor/src/app/(protected)/dashboard/text-to-video/page.tsx"
      to: "editor/src/components/editor/ai/storyboard-wizard.tsx"
      via: "Dashboard page renders StoryboardWizard"
      pattern: "StoryboardWizard"
---

<objective>
Build the storyboard wizard UI where users describe video scenes, select a style preset, and generate a stock-footage-assembled video that opens in the editor.

Purpose: Delivers the user-facing text-to-video experience (AI-04). Per user decision: storyboard approach where user describes scenes in sequence, stock footage + AI assembly, output opens in editor as editable project, curated style/mood presets guide generation.

Output: StoryboardWizard component, SceneEditor component, StylePicker component, storyboard store, dashboard entry page.
</objective>

<execution_context>
@/home/solo/.claude/get-shit-done/workflows/execute-plan.md
@/home/solo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-ai-features/11-CONTEXT.md
@.planning/phases/11-ai-features/11-05-SUMMARY.md

@editor/src/lib/ai/presets/video-style-presets.ts
@editor/src/stores/studio-store.ts
@editor/src/stores/project-store.ts
@editor/src/app/(protected)/dashboard/page.tsx
@editor/src/app/(protected)/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Storyboard store and scene editor components</name>
  <files>
    editor/src/stores/storyboard-store.ts
    editor/src/components/editor/ai/scene-editor.tsx
    editor/src/components/editor/ai/style-picker.tsx
  </files>
  <action>
    Create the storyboard state management and building block components:

    1. `editor/src/stores/storyboard-store.ts` - Storyboard state:
       - Zustand store with the following state:
         ```
         interface StoryboardState {
           scenes: Array<{ id: string; description: string; duration: number; textOverlay: string }>;
           selectedStyleId: string;   // default: 'corporate'
           isGenerating: boolean;
           composition: any | null;   // Generated ProjectComposition
           scenesWithClips: any[];    // Scene-to-clip mapping from API

           addScene: () => void;      // Adds empty scene with default 5s duration
           removeScene: (id: string) => void;
           updateScene: (id: string, updates: Partial<Scene>) => void;
           reorderScenes: (fromIndex: number, toIndex: number) => void;
           setStyle: (styleId: string) => void;
           setGenerating: (value: boolean) => void;
           setComposition: (composition: any, scenesWithClips: any[]) => void;
           reset: () => void;
         }
         ```
       - Initialize with 3 empty scenes: intro (3s), main content (5s), outro (3s)
       - `reorderScenes`: swap scenes by array indices
       - `addScene`: insert at end with `id: crypto.randomUUID()`, duration: 5, empty description
       - No persistence needed (transient creation flow)

    2. `editor/src/components/editor/ai/scene-editor.tsx` - Scene card:
       - Client component
       - Props: `{ scene: Scene; index: number; onUpdate: (updates: Partial<Scene>) => void; onRemove: () => void; clipPreview?: StockClip | null }`
       - Layout (rounded-lg border bg-card p-4):
         - Scene number badge: "Scene {index + 1}" in a small muted pill
         - Description textarea: 2-3 rows, placeholder "Describe what happens in this scene..."
         - Duration input: number input with "s" suffix, min 1, max 30, step 0.5
         - Text overlay input: optional single-line text input, placeholder "Text to display on screen"
         - If clipPreview provided: show thumbnail image (previewUrl) with clip duration and provider badge
         - Remove button (X icon, disabled if only 1 scene)
       - Drag handle icon on the left for reordering (visual only - actual drag handled by parent)

    3. `editor/src/components/editor/ai/style-picker.tsx` - Style preset picker:
       - Client component
       - Props: `{ selectedId: string; onSelect: (id: string) => void }`
       - Import VIDEO_STYLE_PRESETS from `@/lib/ai/presets/video-style-presets`
       - Grid of style cards (3 columns on desktop, 2 on mobile):
         - Each card shows: preset name (bold), description (muted), pacing badge (slow/medium/fast), transition type
         - A small visual indicator: colored bar or icon matching the style mood
         - Selected state: ring-2 ring-primary
         - Hover: border-muted-foreground/50
  </action>
  <verify>
    Run `cd /home/solo/workspace/openvideo && pnpm tsc --noEmit --project editor/tsconfig.json 2>&1 | head -30` to verify TypeScript compiles.
  </verify>
  <done>
    Storyboard store manages scenes array, style selection, and generation state. SceneEditor card provides description, duration, text overlay inputs with clip preview. StylePicker shows 6 style presets in a grid with selection state.
  </done>
</task>

<task type="auto">
  <name>Task 2: Storyboard wizard and dashboard page</name>
  <files>
    editor/src/components/editor/ai/storyboard-wizard.tsx
    editor/src/app/(protected)/dashboard/text-to-video/page.tsx
  </files>
  <action>
    Create the main wizard component and dashboard entry point:

    1. `editor/src/components/editor/ai/storyboard-wizard.tsx` - Multi-step wizard:
       - Client component ('use client')
       - Uses `useStoryboardStore` for state
       - 3-step flow managed by internal `step` state (1, 2, 3):

       **Step 1: Describe Scenes**
       - Header: "Describe Your Video" with subtitle "Add scenes to build your storyboard"
       - List of SceneEditor cards, one per scene from store
       - "Add Scene" button at bottom (plus icon)
       - Simple drag-to-reorder: use array index buttons (move up/move down arrows) instead of full drag library to keep it simple
       - "Next" button (disabled if any scene has empty description)

       **Step 2: Choose Style**
       - Header: "Choose a Style" with subtitle "Select a mood for your video"
       - StylePicker component
       - "Back" and "Next" buttons

       **Step 3: Generate**
       - Header: "Review & Generate"
       - Summary: list of scenes with descriptions, total duration, selected style
       - "Generate Video" button (full width, prominent)
       - On click:
         - Set isGenerating: true
         - POST to `/api/ai/text-to-video` with `{ scenes: store.scenes.map(s => ({ description: s.description, duration: s.duration, textOverlay: s.textOverlay || undefined })), styleId: store.selectedStyleId }`
         - On success: store composition and scenesWithClips in store
         - Show clip previews in scene cards (update step to show results)
         - Show "Open in Editor" button that navigates to editor with composition data
         - Loading state: animated skeleton cards with "Generating..." text
       - "Open in Editor" button on success:
         - Navigate to `/` (editor page) with composition in URL params or sessionStorage
         - Store composition in sessionStorage under key `pendingComposition`
         - On editor load, check sessionStorage for pendingComposition, load clips via `jsonToClip()` and `studio.addClip()`
         - Clear sessionStorage after loading

    2. `editor/src/app/(protected)/dashboard/text-to-video/page.tsx` - Dashboard entry:
       - Server component page
       - Simple page layout matching existing dashboard pages (dashboard header pattern)
       - Title: "Text to Video"
       - Description: "Create videos from text descriptions using AI-powered stock footage assembly"
       - Render `<StoryboardWizard />` as the main content
       - Add navigation link: update the dashboard layout/sidebar to include "Text to Video" link with a Sparkles or Video icon (check existing dashboard nav pattern in layout.tsx)
  </action>
  <verify>
    Run `cd /home/solo/workspace/openvideo && pnpm tsc --noEmit --project editor/tsconfig.json 2>&1 | head -30` to verify TypeScript compiles.
    Verify page exists: `ls editor/src/app/\\(protected\\)/dashboard/text-to-video/page.tsx`
  </verify>
  <done>
    StoryboardWizard has 3 steps: describe scenes, choose style, review and generate. Generation calls /api/ai/text-to-video and shows clip previews. "Open in Editor" navigates to editor with composition data via sessionStorage. Dashboard page at /dashboard/text-to-video provides entry point with navigation link.
  </done>
</task>

</tasks>

<verification>
- Wizard displays 3 steps with proper navigation
- Scenes can be added, edited, removed, and reordered
- Style picker shows 6 presets with selection state
- Generate button calls API and shows loading state
- Clip previews appear next to scenes after generation
- "Open in Editor" navigates and loads composition into Studio
- Dashboard page renders the wizard at /dashboard/text-to-video
</verification>

<success_criteria>
- User can describe 3+ scenes with descriptions and durations
- User can select a style preset for their video
- Generating produces a composition with stock footage clips
- Generated video can be opened in the editor for further editing
- Dashboard provides accessible entry point
</success_criteria>

<output>
After completion, create `.planning/phases/11-ai-features/11-06-SUMMARY.md`
</output>
