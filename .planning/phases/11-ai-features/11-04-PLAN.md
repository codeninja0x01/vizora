---
phase: 11-ai-features
plan: 04
type: execute
wave: 2
depends_on: ["11-03"]
files_modified:
  - editor/src/components/editor/media-panel/subtitle-panel.tsx
  - editor/src/components/editor/media-panel/subtitle-preset-card.tsx
  - editor/src/components/editor/media-panel/store.ts
  - editor/src/components/editor/media-panel/index.tsx
  - editor/src/components/editor/assistant/tools.ts
autonomous: true
must_haves:
  truths:
    - "User can open a Subtitles tab in the media panel"
    - "User can browse and select from 8 subtitle presets with visual previews"
    - "User can choose between karaoke and full phrase animation modes"
    - "Clicking Generate adds subtitle Caption clips to the timeline synced to audio"
    - "User can customize font, color, size, and position after selecting a preset"
  artifacts:
    - path: "editor/src/components/editor/media-panel/subtitle-panel.tsx"
      provides: "Subtitle panel with preset gallery, mode selector, customization options, and generate button"
      exports: ["SubtitlePanel"]
    - path: "editor/src/components/editor/media-panel/subtitle-preset-card.tsx"
      provides: "Preset preview card showing style name, animation mode badge, and visual sample"
      exports: ["SubtitlePresetCard"]
    - path: "editor/src/components/editor/media-panel/store.ts"
      provides: "Updated media panel store with 'subtitles' tab"
  key_links:
    - from: "editor/src/components/editor/media-panel/subtitle-panel.tsx"
      to: "/api/ai/subtitles"
      via: "fetch POST for subtitle generation"
      pattern: "fetch.*api/ai/subtitles"
    - from: "editor/src/components/editor/media-panel/subtitle-panel.tsx"
      to: "editor/src/lib/ai/presets/subtitle-presets.ts"
      via: "Import SUBTITLE_PRESETS for preset gallery display"
      pattern: "SUBTITLE_PRESETS"
    - from: "editor/src/components/editor/media-panel/subtitle-panel.tsx"
      to: "openvideo"
      via: "jsonToClip converts subtitle data to Caption clips, studio.addClip adds to timeline"
      pattern: "jsonToClip|studio\\.addClip"
---

<objective>
Build the subtitle panel UI with preset gallery, animation mode selection, style customization, and subtitle generation that adds Caption clips to the timeline.

Purpose: Delivers the user-facing subtitle experience (AI-02, AI-03). Users browse visual preset cards, pick karaoke or phrase mode, customize styling, and generate subtitles that appear on the timeline synced to their audio. Per user decision: presets + customize, both animation modes, user-draggable positioning, dual subtitle source auto-detection.

Output: SubtitlePanel component, SubtitlePresetCard component, updated media panel with subtitles tab.
</objective>

<execution_context>
@/home/solo/.claude/get-shit-done/workflows/execute-plan.md
@/home/solo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-ai-features/11-CONTEXT.md
@.planning/phases/11-ai-features/11-03-SUMMARY.md

@editor/src/components/editor/media-panel/store.ts
@editor/src/components/editor/media-panel/index.tsx
@editor/src/lib/ai/presets/subtitle-presets.ts
@editor/src/lib/caption-generator.ts
@editor/src/components/editor/assistant/tools.ts
@editor/src/stores/studio-store.ts
@editor/src/stores/timeline-store.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Subtitle preset card and subtitle panel components</name>
  <files>
    editor/src/components/editor/media-panel/subtitle-preset-card.tsx
    editor/src/components/editor/media-panel/subtitle-panel.tsx
  </files>
  <action>
    Create the subtitle UI components:

    1. `editor/src/components/editor/media-panel/subtitle-preset-card.tsx` - Preset preview card:
       - Client component ('use client')
       - Props: `{ preset: SubtitlePreset; selected: boolean; onClick: () => void }`
       - Display a card (rounded-lg border bg-card) showing:
         - Preset name in bold
         - Animation mode badge: "Karaoke" (purple) or "Phrase" (blue) using a small colored pill
         - Visual sample: a small dark rectangle (~200x60) with sample text "Hello World" styled using the preset's font, color, stroke, and shadow CSS equivalents. For karaoke presets, show the first word highlighted with activeFill color.
         - Position indicator: small icon showing top/center/bottom placement
       - Selected state: ring-2 ring-primary border-primary
       - Hover state: border-muted-foreground/50 transition

    2. `editor/src/components/editor/media-panel/subtitle-panel.tsx` - Main subtitle panel:
       - Client component ('use client')
       - Import SUBTITLE_PRESETS from `@/lib/ai/presets/subtitle-presets`
       - State management:
         - `selectedPreset`: currently selected SubtitlePreset (default: getDefaultPreset())
         - `mode`: 'karaoke' | 'phrase' override (initially from preset)
         - `customStyle`: partial style overrides (fontSize, color, fontFamily)
         - `loading`: boolean for generation state
       - Layout (flex col h-full gap-4 p-3):
         - **Section 1: Mode Toggle** - Two buttons side by side: "Karaoke" and "Phrase". Active button has bg-primary. Changing mode filters presets to show matching ones, but also allows override.
         - **Section 2: Preset Gallery** - Grid of SubtitlePresetCard components (2 columns). Clicking a card selects it and updates mode to match preset's mode.
         - **Section 3: Customize** (collapsible section, collapsed by default):
           - Font size slider (24-120, default from preset)
           - Color picker for text color (simple hex input or color swatch)
           - Position dropdown: top, center, bottom
           - Font family select with 5-6 common choices (Inter, Poppins, Bangers, Montserrat, JetBrains Mono)
         - **Section 4: Generate Button** - Full-width button "Generate Subtitles"
           - On click: detect audio source from timeline clips using SubtitleService.detectSource logic (check for voiceover clips by checking clip URLs containing 'voiceovers/')
           - Find audio/video clips in timeline via `useTimelineStore.getState().clips`
           - If no audio/video clips found: show toast error "No audio or video found in timeline"
           - POST to `/api/ai/subtitles` with `{ audioUrl: clipSrc, presetId: selectedPreset.id, mode, videoWidth, videoHeight, format: 'clips' }`
           - On success: iterate response clips, use `jsonToClip()` from openvideo to create Caption clips, add all to studio via `studio.addClip(clips, { trackId: 'track_subtitles_' + Date.now() })`
           - Show loading spinner during generation, success toast on complete
  </action>
  <verify>
    Run `cd /home/solo/workspace/openvideo && pnpm tsc --noEmit --project editor/tsconfig.json 2>&1 | head -30` to verify TypeScript compiles.
  </verify>
  <done>
    SubtitlePresetCard shows visual preview with mode badge and sample text. SubtitlePanel has mode toggle, preset gallery grid, collapsible customization section, and generate button that fetches from /api/ai/subtitles and adds Caption clips to the timeline.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate subtitles tab into media panel and update assistant</name>
  <files>
    editor/src/components/editor/media-panel/store.ts
    editor/src/components/editor/media-panel/index.tsx
    editor/src/components/editor/assistant/tools.ts
  </files>
  <action>
    Wire subtitle panel into the editor and enhance the AI assistant:

    1. `editor/src/components/editor/media-panel/store.ts` - Add subtitles tab:
       - Add 'subtitles' to the `Tab` type union: `| 'subtitles'`
       - Add subtitles entry to `tabs` object:
         ```
         subtitles: {
           icon: IconSubtitles,   // already imported
           label: 'Subtitles',
         },
         ```
       - Note: 'captions' tab already exists. Replace 'captions' with 'subtitles' OR keep both and rename. Since the existing tab is called 'captions', rename it to 'subtitles' - update the Tab type, tabs object key, and label. Keep the icon (IconSubtitles).

    2. `editor/src/components/editor/media-panel/index.tsx` - Render SubtitlePanel:
       - Import `SubtitlePanel` from './subtitle-panel'
       - In the tab content switch/conditional, replace the existing 'captions' case with 'subtitles' case that renders `<SubtitlePanel />`
       - If there's existing caption panel content, preserve it as a sub-section within SubtitlePanel or remove it in favor of the new preset-based approach

    3. `editor/src/components/editor/assistant/tools.ts` - Enhance generate_captions handler:
       - Update `handleGenerateCaptions` to use the new subtitle API:
         - Instead of directly calling `/api/transcribe` and `generateCaptionClips()`, call `/api/ai/subtitles` with `{ audioUrl, presetId: 'modern-karaoke', format: 'clips', videoWidth, videoHeight }`
         - This ensures the assistant uses the same subtitle presets and word timing as the panel
         - Keep the existing clip creation logic (jsonToClip, studio.addClip) but source clip data from the new API response
         - Fall back to the old approach if the new API fails (graceful degradation)
  </action>
  <verify>
    Run `cd /home/solo/workspace/openvideo && pnpm tsc --noEmit --project editor/tsconfig.json 2>&1 | head -30` to verify TypeScript compiles.
    Verify tab exists: `grep -n "subtitles" editor/src/components/editor/media-panel/store.ts`
  </verify>
  <done>
    Media panel has a "Subtitles" tab (replacing or alongside "Captions") that renders SubtitlePanel. AI assistant's generate_captions handler uses the new /api/ai/subtitles endpoint with preset support. Subtitle generation works consistently from both the panel and the assistant.
  </done>
</task>

</tasks>

<verification>
- Subtitles tab appears in the media panel sidebar
- Preset gallery shows 8 cards with visual previews and mode badges
- Mode toggle filters and overrides preset animation mode
- Generate button finds audio clips and sends to subtitle API
- Generated Caption clips appear on timeline synced to audio
- Customization section allows font/color/size/position overrides
- Assistant's caption generation uses the new subtitle system
</verification>

<success_criteria>
- User can open Subtitles tab and see 8 preset cards
- User can select karaoke or phrase mode
- User can customize font, color, size, position
- Clicking Generate creates Caption clips on the timeline
- Subtitles are synced to audio word-level timing
- AI assistant generates subtitles using the same preset system
</success_criteria>

<output>
After completion, create `.planning/phases/11-ai-features/11-04-SUMMARY.md`
</output>
