---
phase: 11-ai-features
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - editor/src/components/editor/media-panel/voiceover-chat-panel.tsx
  - editor/src/components/editor/media-panel/voice-selector.tsx
  - editor/src/stores/generated-store.ts
  - editor/src/components/editor/assistant/tools.ts
  - editor/src/genkit/tools.ts
autonomous: true
must_haves:
  truths:
    - "User can select a voice from a catalog showing ElevenLabs and OpenAI voices"
    - "User can type a script with [pause Xs] markers and generate voiceover"
    - "Generated voiceover audio is added to the timeline as an Audio clip"
    - "User can switch between TTS providers in the voice selector"
  artifacts:
    - path: "editor/src/components/editor/media-panel/voiceover-chat-panel.tsx"
      provides: "Enhanced voiceover panel with voice selection, script input with pause markers, provider toggle"
    - path: "editor/src/components/editor/media-panel/voice-selector.tsx"
      provides: "Voice catalog dropdown with search, provider grouping, and preview"
      exports: ["VoiceSelector"]
    - path: "editor/src/stores/generated-store.ts"
      provides: "Updated generated store with provider and voiceId fields"
  key_links:
    - from: "editor/src/components/editor/media-panel/voiceover-chat-panel.tsx"
      to: "/api/ai/tts"
      via: "fetch POST to synthesis endpoint"
      pattern: "fetch.*api/ai/tts"
    - from: "editor/src/components/editor/media-panel/voice-selector.tsx"
      to: "/api/ai/voices"
      via: "fetch GET to voice listing endpoint"
      pattern: "fetch.*api/ai/voices"
    - from: "editor/src/components/editor/assistant/tools.ts"
      to: "/api/ai/tts"
      via: "handleGenerateVoiceover uses new multi-provider endpoint"
      pattern: "api/ai/tts"
---

<objective>
Enhance the voiceover panel with multi-provider voice selection, script input with pause marker support, and provider switching. Update the AI assistant to use the new TTS API.

Purpose: Delivers the user-facing AI voiceover experience (AI-01). Users can select voices from ElevenLabs or OpenAI, write scripts with pause timing markers, and generate voiceovers that appear on the timeline. Per user decision: single narration track, plain text with [pause 1s] markers, multi-provider voice selection.

Output: Enhanced VoiceoverChatPanel, VoiceSelector component, updated assistant tool handlers.
</objective>

<execution_context>
@/home/solo/.claude/get-shit-done/workflows/execute-plan.md
@/home/solo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-ai-features/11-CONTEXT.md
@.planning/phases/11-ai-features/11-01-SUMMARY.md

@editor/src/components/editor/media-panel/voiceover-chat-panel.tsx
@editor/src/components/editor/media-panel/store.ts
@editor/src/stores/generated-store.ts
@editor/src/components/editor/assistant/tools.ts
@editor/src/genkit/tools.ts
@editor/src/stores/studio-store.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Voice selector component and enhanced voiceover panel</name>
  <files>
    editor/src/components/editor/media-panel/voice-selector.tsx
    editor/src/components/editor/media-panel/voiceover-chat-panel.tsx
    editor/src/stores/generated-store.ts
  </files>
  <action>
    Create VoiceSelector and enhance VoiceoverChatPanel:

    1. `editor/src/components/editor/media-panel/voice-selector.tsx` - Voice catalog component:
       - Client component ('use client')
       - Fetches voices from `GET /api/ai/voices` on mount, caches in component state
       - Shows a dropdown/select using Radix Select or a custom listbox:
         - Group voices by provider (ElevenLabs section, OpenAI section)
         - Each voice item shows: name, provider badge (small colored tag), gender if available
         - Search input at top to filter voices by name
       - Props: `{ value: { provider: string; voiceId: string } | null; onChange: (voice: { provider: string; voiceId: string; name: string }) => void }`
       - Show loading state while fetching, error state if fetch fails
       - Default selection: first ElevenLabs voice if available, else first OpenAI voice
       - Use existing UI patterns: match the styling of other dropdowns in the media panel

    2. `editor/src/components/editor/media-panel/voiceover-chat-panel.tsx` - Enhanced panel:
       - Replace the existing simple text+generate panel with:
         - VoiceSelector at the top for voice selection
         - Script textarea (existing) with added help text: "Use [pause 1s] to add timing pauses"
         - Show character count below textarea (max 5000)
         - Generate button (existing) that now posts to `/api/ai/tts` instead of `/api/elevenlabs/voiceover`
         - Request body: `{ text, voiceId: selectedVoice.voiceId, provider: selectedVoice.provider }`
         - Loading state with progress indication ("Generating voiceover...")
         - On success: add to generated store with provider and voiceId metadata, show toast
         - Below the generator: list of previously generated voiceovers from `useGeneratedStore` with play button and "Add to timeline" button
         - "Add to timeline" creates an Audio clip from the URL and adds via `studio.addClip()`
       - Preserve existing layout structure (flex col, h-full, bg-card)

    3. `editor/src/stores/generated-store.ts` - Add provider metadata:
       - Extend `GeneratedAsset` interface to include: `provider?: string`, `voiceId?: string`, `voiceName?: string`
       - These are optional to maintain backward compatibility with existing stored assets
       - No other changes needed to the store logic
  </action>
  <verify>
    Run `cd /home/solo/workspace/openvideo && pnpm tsc --noEmit --project editor/tsconfig.json 2>&1 | head -30` to verify TypeScript compiles.
  </verify>
  <done>
    VoiceSelector shows voices grouped by provider with search. VoiceoverChatPanel includes voice selection, script textarea with pause marker help text, character counter, and uses the new /api/ai/tts endpoint. Generated voiceovers list below with play and add-to-timeline buttons. GeneratedAsset includes optional provider metadata.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update AI assistant voiceover tool to use multi-provider TTS</name>
  <files>
    editor/src/components/editor/assistant/tools.ts
    editor/src/genkit/tools.ts
  </files>
  <action>
    Update the assistant's voiceover handling to use the new multi-provider API:

    1. `editor/src/components/editor/assistant/tools.ts` - Update `handleGenerateVoiceover`:
       - Change fetch URL from `/api/elevenlabs/voiceover` to `/api/ai/tts`
       - Update request body to include `provider` field (default to 'elevenlabs' if not specified by AI)
       - Update request body shape: `{ text, voiceId: voiceId || '21m00Tcm4TlvDq8ikWAM', provider: provider || 'elevenlabs' }`
       - Response shape is the same (url field), no other changes needed
       - Keep the same clip creation logic (Audio.fromUrl, set display timing, studio.addClip)

    2. `editor/src/genkit/tools.ts` - Update generate_voiceover tool schema:
       - Add `provider` parameter to the generate_voiceover tool inputSchema:
         `provider: z.enum(['elevenlabs', 'openai']).optional().describe('TTS provider to use (default: elevenlabs)')`
       - Keep existing voiceId, text, targetId, from parameters
       - No changes to the tool output or handler logic (it returns metadata for client-side execution)
  </action>
  <verify>
    Run `cd /home/solo/workspace/openvideo && pnpm tsc --noEmit --project editor/tsconfig.json 2>&1 | head -30` to verify TypeScript compiles.
    Grep to confirm new endpoint is used: `grep -r "api/ai/tts" editor/src/components/editor/assistant/`
  </verify>
  <done>
    AI assistant's generate_voiceover tool accepts optional provider parameter. handleGenerateVoiceover function uses /api/ai/tts endpoint with provider support. Existing voiceover generation via assistant still works (defaults to elevenlabs).
  </done>
</task>

</tasks>

<verification>
- VoiceSelector fetches and displays voices from both providers
- VoiceoverChatPanel sends requests to /api/ai/tts with correct provider
- Generated voiceovers appear in the panel list with play buttons
- "Add to timeline" creates Audio clip on the Studio timeline
- Assistant voiceover tool uses the new multi-provider endpoint
- Pause markers in scripts are preserved in the text sent to the API
</verification>

<success_criteria>
- User can open voiceover panel, see voices from ElevenLabs and OpenAI
- User can type script with [pause 1s] markers and generate voiceover
- Generated audio appears in the generated assets list
- User can add generated voiceover to the timeline
- AI assistant can generate voiceovers with provider selection
</success_criteria>

<output>
After completion, create `.planning/phases/11-ai-features/11-02-SUMMARY.md`
</output>
