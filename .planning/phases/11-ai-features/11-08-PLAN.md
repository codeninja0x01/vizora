---
phase: 11-ai-features
plan: 08
type: execute
wave: 2
depends_on: ["11-07"]
files_modified:
  - editor/src/components/editor/ai/template-chat.tsx
  - editor/src/components/editor/ai/template-style-picker.tsx
  - editor/src/stores/template-generation-store.ts
  - editor/src/components/editor/media-panel/store.ts
  - editor/src/components/editor/media-panel/index.tsx
autonomous: true
must_haves:
  truths:
    - "User can trigger template generation from inside the editor"
    - "User can describe a template in free text and select a style preset"
    - "Generated template elements appear on the editor canvas"
    - "User can refine the template through conversational chat (e.g., 'Make the intro shorter')"
    - "Auto-detected merge fields are shown and can be adjusted"
  artifacts:
    - path: "editor/src/components/editor/ai/template-chat.tsx"
      provides: "Template generation chat interface with prompt input, style picker, and refinement conversation"
      exports: ["TemplateChat"]
    - path: "editor/src/components/editor/ai/template-style-picker.tsx"
      provides: "Template style preset picker for the generation flow"
      exports: ["TemplateStylePicker"]
    - path: "editor/src/stores/template-generation-store.ts"
      provides: "Zustand store for template generation state, conversation history, merge fields"
      exports: ["useTemplateGenerationStore"]
  key_links:
    - from: "editor/src/components/editor/ai/template-chat.tsx"
      to: "/api/ai/template"
      via: "fetch POST for initial template generation"
      pattern: "fetch.*api/ai/template"
    - from: "editor/src/components/editor/ai/template-chat.tsx"
      to: "/api/ai/template/refine"
      via: "fetch POST for iterative refinement"
      pattern: "fetch.*api/ai/template/refine"
    - from: "editor/src/components/editor/ai/template-chat.tsx"
      to: "openvideo"
      via: "jsonToClip converts template elements to Studio clips for canvas display"
      pattern: "jsonToClip|studio\\.addClip|studio\\.removeClip"
---

<objective>
Build the template generation chat interface inside the editor where users describe templates, select styles, generate production-ready templates that appear on the canvas, and iteratively refine them through conversation.

Purpose: Delivers the user-facing template auto-generation experience (AI-05). Per user decision: editor command entry point, prompt + style picker, iterative chat refinement ("Make the intro shorter"), auto-detect merge fields, full element palette, always best quality. Template generation should feel like having an AI design assistant inside the editor.

Output: TemplateChat component, TemplateStylePicker, template generation store, editor panel integration.
</objective>

<execution_context>
@/home/solo/.claude/get-shit-done/workflows/execute-plan.md
@/home/solo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-ai-features/11-CONTEXT.md
@.planning/phases/11-ai-features/11-07-SUMMARY.md

@editor/src/lib/ai/presets/template-style-presets.ts
@editor/src/components/editor/assistant/assistant.tsx
@editor/src/components/editor/media-panel/store.ts
@editor/src/components/editor/media-panel/index.tsx
@editor/src/stores/studio-store.ts
@editor/src/stores/template-store.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Template generation store and style picker</name>
  <files>
    editor/src/stores/template-generation-store.ts
    editor/src/components/editor/ai/template-style-picker.tsx
  </files>
  <action>
    Create the state management and style selection component:

    1. `editor/src/stores/template-generation-store.ts` - Generation state:
       - Zustand store (no persistence - session-based):
         ```
         interface TemplateGenerationState {
           // Generation state
           selectedStyleId: string;        // default: 'corporate'
           isGenerating: boolean;
           generatedTemplate: any | null;  // Current generated template JSON
           mergeFields: MergeFieldSuggestion[];
           conversationId: string | null;

           // Chat state
           messages: Array<{
             role: 'user' | 'assistant';
             content: string;
             timestamp: number;
           }>;

           // Actions
           setStyle: (styleId: string) => void;
           setGenerating: (value: boolean) => void;
           setTemplate: (template: any, mergeFields: MergeFieldSuggestion[], conversationId: string) => void;
           addMessage: (role: 'user' | 'assistant', content: string) => void;
           reset: () => void;
           removeMergeField: (elementId: string, property: string) => void;
           addMergeField: (field: MergeFieldSuggestion) => void;
         }
         ```
       - Import MergeFieldSuggestion type from `@/lib/ai/utils/merge-field-detector`

    2. `editor/src/components/editor/ai/template-style-picker.tsx` - Style picker for templates:
       - Client component
       - Props: `{ selectedId: string; onSelect: (id: string) => void; compact?: boolean }`
       - Import TEMPLATE_STYLE_PRESETS from `@/lib/ai/presets/template-style-presets`
       - Two display modes:
         - Default: Grid of cards (3 cols) showing name, description, color palette swatches (5 small colored circles), mood badge
         - Compact mode (compact=true): Horizontal scrollable row of smaller cards for embedding in the chat panel
       - Each card shows:
         - Style name (bold, small text)
         - 3-5 color palette circles (8px each, from preset.colorPalette)
         - Aspect ratio badge (16:9, 9:16, 1:1) - small muted text
       - Selected: ring-2 ring-primary, bg-primary/5
       - Hover: border-muted-foreground/50
  </action>
  <verify>
    Run `cd /home/solo/workspace/openvideo && pnpm tsc --noEmit --project editor/tsconfig.json 2>&1 | head -30` to verify TypeScript compiles.
  </verify>
  <done>
    Template generation store manages style selection, generation state, chat messages, merge fields, and conversation ID. TemplateStylePicker displays 12 style presets in grid or compact scrollable row with color palette previews.
  </done>
</task>

<task type="auto">
  <name>Task 2: Template chat component and editor panel integration</name>
  <files>
    editor/src/components/editor/ai/template-chat.tsx
    editor/src/components/editor/media-panel/store.ts
    editor/src/components/editor/media-panel/index.tsx
  </files>
  <action>
    Create the template generation chat and wire into the editor:

    1. `editor/src/components/editor/ai/template-chat.tsx` - Chat interface:
       - Client component ('use client')
       - Uses `useTemplateGenerationStore` for state
       - Uses `useStudioStore` for studio access
       - Layout (flex col h-full):

       **Initial state (no template generated yet):**
       - Header: "AI Template Generator" with sparkles icon
       - TemplateStylePicker in compact mode
       - Prompt input area:
         - Textarea: "Describe the template you want to create..."
         - Placeholder examples below: "A product launch video with logo reveal", "Social media ad for a restaurant", "Corporate presentation with data slides"
         - "Generate" button (full width)
       - On Generate click:
         - Add user message to store messages
         - Set isGenerating: true
         - POST to `/api/ai/template` with `{ prompt, styleId: store.selectedStyleId }`
         - On success: store template, mergeFields, conversationId in store
         - Add assistant message: "Template generated! Here's what I created: {template.name} - {template.description}"
         - Apply template to canvas:
           - Clear existing clips: `studio.clips.forEach(c => studio.removeClip(c.id))`
           - Convert each template element to clip JSON compatible with openvideo format:
             - Text elements: create Text clip with style properties
             - Image elements: create Image clip (use placeholder image URL like https://placehold.co/800x600)
             - Shape elements: create Text clip with background color as approximation
           - Use `jsonToClip()` and `studio.addClip()` for each element
           - Set display timing from element.timing (convert seconds to microseconds)
         - Show merge fields section below

       **After generation (template exists):**
       - Chat message history (scrollable):
         - User messages aligned right, assistant messages aligned left
         - Same styling as existing assistant.tsx chat bubbles
       - Merge fields section (collapsible):
         - List of detected merge fields with name, type badge, element preview
         - Each field has a remove (X) button to exclude from merge fields
         - Small "Add merge field" button at bottom (for manual additions)
       - Refinement input at bottom:
         - Textarea: "Refine your template... e.g., 'Make the intro shorter', 'Change the font to something bolder'"
         - "Refine" button
         - On Refine click:
           - Add user message to store
           - POST to `/api/ai/template/refine` with `{ conversationId: store.conversationId, prompt: refinementText, currentTemplate: store.generatedTemplate }`
           - On success: update template and mergeFields in store
           - Re-apply template to canvas (clear and re-add)
           - Add assistant message: "Template updated based on your feedback."
       - "Save as Template" button at the very bottom:
         - Opens the existing save-template-dialog from the editor
         - Pre-populates with the generated template data

    2. `editor/src/components/editor/media-panel/store.ts` - Add AI Generate tab:
       - Add 'ai-generate' to the `Tab` type union
       - Add to `tabs` object:
         ```
         'ai-generate': {
           icon: IconSparkles,  // from @tabler/icons-react (already imported)
           label: 'AI Generate',
         },
         ```
       - Note: This uses the existing IconSparkles which is already imported for effects

    3. `editor/src/components/editor/media-panel/index.tsx` - Render TemplateChat:
       - Import TemplateChat from `@/components/editor/ai/template-chat`
       - Add case for 'ai-generate' tab that renders `<TemplateChat />`
       - Position the AI Generate tab near the top of the tab list (after uploads, before text)
  </action>
  <verify>
    Run `cd /home/solo/workspace/openvideo && pnpm tsc --noEmit --project editor/tsconfig.json 2>&1 | head -30` to verify TypeScript compiles.
    Verify tab integration: `grep -n "ai-generate" editor/src/components/editor/media-panel/store.ts`
  </verify>
  <done>
    TemplateChat provides full generation and refinement flow: style picker, prompt input, generation with canvas application, chat-based refinement, merge field management, and save-as-template integration. "AI Generate" tab appears in the media panel sidebar. Generated templates are applied to the editor canvas with clips created from template elements.
  </done>
</task>

</tasks>

<verification>
- "AI Generate" tab appears in the media panel
- Style picker shows 12 presets with color swatches
- Generate button creates template and applies elements to canvas
- Chat messages display user prompts and assistant responses
- Refinement updates template without losing context
- Merge fields section shows detected fields with remove capability
- Template elements appear as clips on the timeline/canvas
</verification>

<success_criteria>
- User can open AI Generate tab and describe a template
- User can select a style preset for the template
- Generated template elements appear on the editor canvas
- User can refine via chat: "Make the intro shorter" updates the template
- Auto-detected merge fields are displayed and adjustable
- Template can be saved via the existing save-template dialog
</success_criteria>

<output>
After completion, create `.planning/phases/11-ai-features/11-08-SUMMARY.md`
</output>
