---
phase: 03-template-system
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - editor/src/components/editor/header.tsx
  - editor/src/components/editor/save-template-dialog.tsx
  - editor/src/stores/template-store.ts
  - editor/src/components/editor/template-mode/template-bar.tsx
  - editor/src/components/editor/template-mode/merge-field-panel.tsx
  - editor/src/components/editor/editor.tsx
autonomous: true

must_haves:
  truths:
    - "User can click 'Save as Template' in the editor File menu to create a template from the current project"
    - "User can name the template and select a category before saving"
    - "Editor shows a template mode indicator bar when editing a template"
    - "User can toggle merge field markers on individual element properties in the properties panel area"
  artifacts:
    - path: "editor/src/components/editor/save-template-dialog.tsx"
      provides: "Save as Template dialog with name, description, category inputs"
      min_lines: 50
    - path: "editor/src/stores/template-store.ts"
      provides: "Zustand store for template mode state"
      exports: ["useTemplateStore"]
    - path: "editor/src/components/editor/template-mode/template-bar.tsx"
      provides: "Template mode indicator bar at top of editor"
      min_lines: 20
    - path: "editor/src/components/editor/template-mode/merge-field-panel.tsx"
      provides: "Panel for marking/unmarking merge fields on selected element"
      min_lines: 40
  key_links:
    - from: "editor/src/components/editor/header.tsx"
      to: "editor/src/components/editor/save-template-dialog.tsx"
      via: "File menu dropdown item opens dialog"
      pattern: "SaveTemplateDialog|save-template"
    - from: "editor/src/components/editor/save-template-dialog.tsx"
      to: "editor/src/app/(protected)/dashboard/templates/actions.ts"
      via: "calls createTemplate server action on submit"
      pattern: "createTemplate"
    - from: "editor/src/stores/template-store.ts"
      to: "editor/src/types/template.ts"
      via: "stores MergeField array and template metadata"
      pattern: "MergeField"
    - from: "editor/src/components/editor/editor.tsx"
      to: "editor/src/components/editor/template-mode/template-bar.tsx"
      via: "conditionally renders template bar when in template mode"
      pattern: "TemplateBar|templateMode"
---

<objective>
Add "Save as Template" functionality to the editor and implement template mode with merge field marking capabilities.

Purpose: This is the primary user-facing feature for template creation. Users convert their video projects into reusable templates directly from the editor, and can mark which elements/properties become dynamic merge fields.
Output: Save as Template dialog in editor header, template mode indicator bar, merge field marking panel, template state management via Zustand store.
</objective>

<execution_context>
@/home/solo/.claude/get-shit-done/workflows/execute-plan.md
@/home/solo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-template-system/03-CONTEXT.md
@.planning/phases/03-template-system/03-02-SUMMARY.md
@editor/src/components/editor/header.tsx
@editor/src/components/editor/editor.tsx
@editor/src/stores/studio-store.ts
@editor/src/stores/panel-store.ts
@editor/src/types/template.ts
@editor/src/lib/merge-fields.ts
@editor/src/app/(protected)/dashboard/templates/actions.ts
@packages/openvideo/src/json-serialization.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Save as Template dialog and template store</name>
  <files>editor/src/components/editor/header.tsx, editor/src/components/editor/save-template-dialog.tsx, editor/src/stores/template-store.ts</files>
  <action>
**1. Create editor/src/stores/template-store.ts** - Zustand store for template mode state:

```typescript
interface TemplateState {
  // Template mode
  isTemplateMode: boolean;
  templateId: string | null;        // ID of template being edited (null for new)
  templateName: string;

  // Merge fields being marked
  markedFields: Array<{ elementId: string; property: string }>;

  // Actions
  enterTemplateMode: (templateId: string | null, name: string) => void;
  exitTemplateMode: () => void;
  toggleMergeField: (elementId: string, property: string) => void;
  isFieldMarked: (elementId: string, property: string) => boolean;
  clearMarkedFields: () => void;
  setMarkedFields: (fields: Array<{ elementId: string; property: string }>) => void;
}
```

The `toggleMergeField` action should add the {elementId, property} pair if not present, or remove it if already present. The `isFieldMarked` selector checks if a given elementId+property combo exists in markedFields.

**2. Create editor/src/components/editor/save-template-dialog.tsx** - Client component dialog:

- Accept props: `open: boolean`, `onOpenChange: (open: boolean) => void`
- Form fields: name (required text input), description (optional textarea), category (select dropdown using TEMPLATE_CATEGORIES from types/template.ts)
- On submit:
  1. Get studio from useStudioStore
  2. Call studio.exportToJSON() to snapshot the project data
  3. Capture thumbnail from studio canvas: get the canvas element, call canvas.toDataURL('image/png') for a base64 thumbnail
  4. Get markedFields from useTemplateStore
  5. Call extractMergeFields(projectData, markedFields) from merge-fields.ts
  6. Call createTemplate server action with { name, description, thumbnailUrl (base64), projectData, mergeFields, category, tags: [] }
  7. Show success toast via sonner
  8. Enter template mode with the returned template ID
  9. Close dialog
- Style using existing UI components: Dialog from @/components/ui/dialog, Button, Input, Label from Radix UI primitives. Follow the dark theme (bg-white/5, border-white/10, text-foreground). Reference create-dialog.tsx from api-keys for patterns.

**3. Modify editor/src/components/editor/header.tsx**:
- Add a new DropdownMenuItem in the File menu dropdown (after "Import from JSON"):
  ```
  <DropdownMenuItem onClick={() => setSaveTemplateOpen(true)}>
    <Layout className="mr-2 size-4" />
    <span>Save as Template</span>
  </DropdownMenuItem>
  ```
- Add state: `const [isSaveTemplateOpen, setIsSaveTemplateOpen] = useState(false)`
- Render SaveTemplateDialog at the bottom of the header component (alongside ExportModal and ShortcutsModal)
- Import Layout icon from lucide-react
- Import SaveTemplateDialog from './save-template-dialog'

Per user decision: "Save as Template button inside the existing editor - user converts while working on a project". The original project is preserved - the template is a new entity from a snapshot.
  </action>
  <verify>Run `cd /home/solo/workspace/openvideo/editor && npx tsc --noEmit --pretty 2>&1 | head -30`. Verify the File menu in header.tsx has the new "Save as Template" item. Verify save-template-dialog.tsx exists and imports createTemplate. Verify template-store.ts exports useTemplateStore.</verify>
  <done>Editor header File menu has "Save as Template" option. Save dialog captures project snapshot via studio.exportToJSON(), thumbnail via canvas.toDataURL(), and calls createTemplate server action. Template store manages template mode state and merge field marking. All files compile.</done>
</task>

<task type="auto">
  <name>Task 2: Implement template mode indicator and merge field panel</name>
  <files>editor/src/components/editor/template-mode/template-bar.tsx, editor/src/components/editor/template-mode/merge-field-panel.tsx, editor/src/components/editor/editor.tsx</files>
  <action>
**1. Create editor/src/components/editor/template-mode/template-bar.tsx** - Template mode indicator bar:

- A thin horizontal bar that appears at the very top of the editor (above the Header) when isTemplateMode is true
- Shows: "Template Mode" label with a Layers icon, template name, and an "Exit Template Mode" button
- Style: bg-primary/20 (indigo tint), text-sm, h-8, flex items-center justify-between px-4
- Exit button calls exitTemplateMode() from useTemplateStore and optionally navigates back to dashboard
- This bar gives clear visual feedback that the user is in template editing context per user decision: "template mode overlay showing merge field markers"

**2. Create editor/src/components/editor/template-mode/merge-field-panel.tsx** - Merge field marking panel:

- Renders when isTemplateMode is true AND a clip is selected (from useStudioStore selectedClips)
- Gets the selected clip's type and ID
- Calls getMergeableProperties(clipType) to get available properties
- For each property, renders a row with:
  - Property display name (capitalize, e.g., 'fontSize' -> 'Font Size')
  - Current value (read from the clip object)
  - A Switch/Toggle component (from Radix UI) to mark/unmark as merge field
  - Toggle calls toggleMergeField(elementId, property) from useTemplateStore
  - Checked state from isFieldMarked(elementId, property)
- When a property is toggled ON, it becomes a merge field (shown with a small tag icon indicator)
- Group properties by category: "Content" (text, src), "Position" (left, top, width, height), "Style" (fontSize, fontFamily, color, opacity, volume)
- Style: match existing properties panel aesthetic (bg-[var(--panel-background)], text-sm, divide-y divide-white/5)

Per Claude's discretion: implementing merge field marking as toggles in a dedicated "Merge Fields" section. This appears contextually when a clip is selected in template mode, alongside the existing properties panel.

**3. Modify editor/src/components/editor/editor.tsx**:

- Import useTemplateStore and check isTemplateMode
- Import TemplateBar from './template-mode/template-bar'
- Import MergeFieldPanel from './template-mode/merge-field-panel'
- Add TemplateBar above the Header component (conditionally rendered when isTemplateMode is true):
  ```jsx
  {isTemplateMode && <TemplateBar />}
  <Header />
  ```
- Add MergeFieldPanel as a floating panel or sidebar section that appears when in template mode with a clip selected. Position it within the MediaPanel area or as an overlay near the properties panel. One approach: render it at the bottom of the left panel (MediaPanel area) as a collapsible section.
- Ensure the existing editor functionality is not affected when template mode is OFF (default state)

Also support loading a template into the editor via URL search params. Check for `?templateId=xxx` in the URL (using useSearchParams from next/navigation). If present:
- Fetch the template via getTemplateById server action
- Call studio.loadFromJSON(template.projectData) to load the project data
- Enter template mode with the template ID and name
- Set markedFields from template.mergeFields (map each to {elementId, property})
This enables editing existing templates and the clone flow.
  </action>
  <verify>Run `cd /home/solo/workspace/openvideo/editor && npx tsc --noEmit --pretty 2>&1 | head -30`. Verify template-bar.tsx renders template mode indicator. Verify merge-field-panel.tsx renders toggles for mergeable properties. Verify editor.tsx conditionally renders template mode components.</verify>
  <done>Template mode indicator bar appears at top of editor when in template mode. Merge field panel shows toggleable properties for selected clips. Editor supports loading templates via ?templateId URL param. Template mode state managed through useTemplateStore. Editor functionality unaffected when template mode is OFF.</done>
</task>

</tasks>

<verification>
1. Header File menu includes "Save as Template" option
2. Save dialog captures project snapshot and calls createTemplate
3. Template store provides isTemplateMode, markedFields state
4. Template bar appears at top of editor in template mode
5. Merge field panel shows toggleable properties per clip type
6. Editor loads template data from ?templateId URL param
7. TypeScript compiles without errors
</verification>

<success_criteria>
- User can open Save as Template dialog from editor File menu
- Dialog captures project data, thumbnail, and marked merge fields
- Template mode visual indicator clearly distinguishes template editing from normal editing
- Merge field toggles work for all mergeable properties per clip type
- Existing editor functionality is unaffected when template mode is OFF
</success_criteria>

<output>
After completion, create `.planning/phases/03-template-system/03-03-SUMMARY.md`
</output>
