---
phase: 03-template-system
plan: 04
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - editor/src/app/(protected)/dashboard/templates/page.tsx
  - editor/src/app/(protected)/dashboard/templates/template-card.tsx
  - editor/src/app/(protected)/dashboard/templates/delete-button.tsx
  - editor/src/app/(protected)/dashboard/page.tsx
  - editor/src/app/(protected)/layout.tsx
autonomous: true

must_haves:
  truths:
    - "User can see a list of their templates on the dashboard"
    - "User can delete templates they own from the dashboard"
    - "User can navigate to templates and gallery from the dashboard navigation"
    - "Dashboard shows template cards with thumbnail, name, and merge field count"
  artifacts:
    - path: "editor/src/app/(protected)/dashboard/templates/page.tsx"
      provides: "My Templates listing page"
      min_lines: 30
    - path: "editor/src/app/(protected)/dashboard/templates/template-card.tsx"
      provides: "Reusable template card component"
      min_lines: 25
    - path: "editor/src/app/(protected)/dashboard/templates/delete-button.tsx"
      provides: "Delete template button with confirmation"
      min_lines: 20
  key_links:
    - from: "editor/src/app/(protected)/dashboard/templates/page.tsx"
      to: "editor/src/app/(protected)/dashboard/templates/actions.ts"
      via: "calls getTemplates server action"
      pattern: "getTemplates"
    - from: "editor/src/app/(protected)/dashboard/templates/delete-button.tsx"
      to: "editor/src/app/(protected)/dashboard/templates/actions.ts"
      via: "calls deleteTemplate server action"
      pattern: "deleteTemplate"
    - from: "editor/src/app/(protected)/layout.tsx"
      to: "dashboard/templates"
      via: "nav link to Templates page"
      pattern: "href.*dashboard/templates"
---

<objective>
Create the "My Templates" section in the dashboard where users can view, manage, and navigate to their templates.

Purpose: Users need a central place to see all templates they've created, access them for editing, and delete ones they no longer need. This also updates the dashboard navigation to include template and gallery links.
Output: Templates listing page, reusable template card component, delete functionality, updated dashboard navigation.
</objective>

<execution_context>
@/home/solo/.claude/get-shit-done/workflows/execute-plan.md
@/home/solo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-template-system/03-CONTEXT.md
@.planning/phases/03-template-system/03-02-SUMMARY.md
@editor/src/app/(protected)/dashboard/page.tsx
@editor/src/app/(protected)/layout.tsx
@editor/src/app/(protected)/dashboard/api-keys/page.tsx
@editor/src/app/(protected)/dashboard/api-keys/revoke-button.tsx
@editor/src/types/template.ts
@editor/src/app/(protected)/dashboard/templates/actions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create My Templates page with template cards</name>
  <files>editor/src/app/(protected)/dashboard/templates/page.tsx, editor/src/app/(protected)/dashboard/templates/template-card.tsx</files>
  <action>
**1. Create editor/src/app/(protected)/dashboard/templates/template-card.tsx** - Client component:

A reusable card component that displays a single template. Props: template object (with id, name, description, thumbnailUrl, mergeFields, category, updatedAt).

Card layout per user decision ("static thumbnail, template name, merge field count -- minimal and clean"):
- Top section: thumbnail image (if thumbnailUrl exists, render as img with object-cover; if null, show a placeholder gradient with a Layers icon)
- Aspect ratio: 16:9 for the thumbnail area
- Below thumbnail: template name (font-medium, truncate), merge field count badge (e.g., "3 fields" in a small rounded badge with bg-primary/10 text-primary)
- Optional: category badge if present, using TEMPLATE_CATEGORIES display names
- Bottom: relative time since last update (e.g., "Updated 2 hours ago") using the same relative time formatting pattern from api-keys
- Hover: slight scale transform (scale-[1.02]) and shadow elevation
- Click: navigates to the editor with `/?templateId={id}` to open in template mode for editing

Style following the dark theme: bg-card, border border-border, rounded-lg overflow-hidden. Use the Card component from @/components/ui/card.

**2. Create editor/src/app/(protected)/dashboard/templates/page.tsx** - Server component:

Per user decision: separate section for "My Templates" (user dashboard).

- Call getTemplates() server action to fetch user's templates
- Page header: "My Templates" title with description text
- If no templates: show an empty state with a message ("No templates yet. Create one from the editor by clicking File > Save as Template.") and a link to the editor
- If templates exist: render in a responsive grid (grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6)
- Each template renders a TemplateCard component
- Add a "Create Template" link/button at the top right that links to the editor (href="/")

Follow the same page structure as the api-keys page (heading, description, content grid).
  </action>
  <verify>Run `cd /home/solo/workspace/openvideo/editor && npx tsc --noEmit --pretty 2>&1 | head -30`. Verify templates/page.tsx is a server component that calls getTemplates. Verify template-card.tsx renders thumbnail, name, and field count.</verify>
  <done>My Templates page exists at /dashboard/templates showing a grid of template cards. Each card displays thumbnail (or placeholder), template name, merge field count badge, and last updated time. Empty state shown when no templates exist. Cards link to editor in template mode for editing.</done>
</task>

<task type="auto">
  <name>Task 2: Add delete functionality and update dashboard navigation</name>
  <files>editor/src/app/(protected)/dashboard/templates/delete-button.tsx, editor/src/app/(protected)/dashboard/page.tsx, editor/src/app/(protected)/layout.tsx</files>
  <action>
**1. Create editor/src/app/(protected)/dashboard/templates/delete-button.tsx** - Client component:

Follow the same pattern as editor/src/app/(protected)/dashboard/api-keys/revoke-button.tsx:
- Props: templateId: string, templateName: string
- Renders a Button with Trash2 icon
- On click, shows confirmation dialog (AlertDialog from Radix UI): "Delete template '{name}'? This action cannot be undone."
- On confirm: calls deleteTemplate(templateId) server action
- Shows loading state during deletion
- Shows success/error toast via sonner
- Style: destructive variant button, small size

Also update template-card.tsx to include the DeleteButton in the card footer (bottom-right corner), with a click handler that stops event propagation (so clicking delete doesn't navigate to the template).

**2. Modify editor/src/app/(protected)/dashboard/page.tsx**:

Add a "Templates" card to the dashboard grid (alongside the existing "Account Info" and "API Keys" cards):
- Card with Link wrapping to "/dashboard/templates"
- Icon: Layers from lucide-react
- Title: "Templates"
- Description: "Create and manage reusable video templates with dynamic merge fields"
- Same hover transition as the API Keys card (hover:bg-accent/50)

Also add a "Template Gallery" card:
- Link to "/gallery"
- Icon: LayoutGrid from lucide-react
- Title: "Template Gallery"
- Description: "Browse pre-built templates and clone them to your account"

**3. Modify editor/src/app/(protected)/layout.tsx**:

Add navigation links to the header nav (in the `hidden space-x-4 md:flex` div):
- "Templates" linking to "/dashboard/templates" (after "API Keys")
- "Gallery" linking to "/gallery" (after "Templates")

Both links use the same style as existing nav links: `text-sm text-muted-foreground hover:text-foreground transition-colors`.
  </action>
  <verify>Run `cd /home/solo/workspace/openvideo/editor && npx tsc --noEmit --pretty 2>&1 | head -30`. Verify layout.tsx has Templates and Gallery nav links. Verify dashboard page.tsx has Templates and Gallery cards. Verify delete-button.tsx calls deleteTemplate.</verify>
  <done>Delete button with confirmation dialog exists on template cards. Dashboard page includes Templates and Gallery cards in the grid. Protected layout navigation has Templates and Gallery links. All components follow established dark theme patterns.</done>
</task>

</tasks>

<verification>
1. /dashboard/templates page renders template grid or empty state
2. Template cards show thumbnail, name, field count, and last updated
3. Delete button shows confirmation and calls deleteTemplate
4. Dashboard page has Templates and Gallery cards
5. Navigation bar has Templates and Gallery links
6. TypeScript compiles without errors
</verification>

<success_criteria>
- User can navigate to My Templates from dashboard nav
- Templates page shows user's templates in a card grid
- Each card displays thumbnail (or placeholder), name, and merge field count
- User can delete templates with confirmation dialog
- Dashboard homepage includes template and gallery shortcuts
</success_criteria>

<output>
After completion, create `.planning/phases/03-template-system/03-04-SUMMARY.md`
</output>
