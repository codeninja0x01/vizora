---
phase: 01-editor-polish
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - editor/src/components/editor/header.tsx
  - editor/src/components/editor/timeline/timeline-toolbar.tsx
  - editor/src/components/editor/timeline/timeline-ruler.tsx
autonomous: true

must_haves:
  truths:
    - "Header toolbar has clean, modern styling with purple accent on primary actions"
    - "Timeline toolbar uses consistent icon sizes and modern button styling"
    - "Playback controls (play/pause, skip) are visually prominent and centered"
    - "Time display uses mm:ss format consistently"
    - "All icons across header and timeline toolbar use consistent sizing (16px for toolbar, 20px for primary actions)"
  artifacts:
    - path: "editor/src/components/editor/header.tsx"
      provides: "Polished header with modern styling, consistent icons, purple accent"
      contains: "header"
    - path: "editor/src/components/editor/timeline/timeline-toolbar.tsx"
      provides: "Polished timeline toolbar with modern playback controls"
      contains: "TimelineToolbar"
  key_links:
    - from: "editor/src/components/editor/header.tsx"
      to: "editor/src/app/globals.css"
      via: "Tailwind classes referencing design tokens"
      pattern: "bg-primary|text-primary|accent"
    - from: "editor/src/components/editor/timeline/timeline-toolbar.tsx"
      to: "editor/src/app/globals.css"
      via: "Tailwind classes referencing design tokens"
      pattern: "bg-|text-|border-"
---

<objective>
Polish the header toolbar and timeline toolbar with modern styling, consistent icon sizing, and purple accent colors to match the new design system.

Purpose: Toolbars are the most frequently seen and interacted-with UI elements. Polishing them creates immediate visual impact and establishes the quality standard for the rest of the editor.
Output: Modernized header.tsx and timeline-toolbar.tsx with consistent, professional styling.
</objective>

<execution_context>
@/home/solo/.claude/get-shit-done/workflows/execute-plan.md
@/home/solo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-editor-polish/01-CONTEXT.md
@.planning/phases/01-editor-polish/01-RESEARCH.md
@.planning/phases/01-editor-polish/01-01-SUMMARY.md
@editor/src/components/editor/header.tsx
@editor/src/components/editor/timeline/timeline-toolbar.tsx
@editor/src/components/editor/timeline/timeline-ruler.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish header toolbar with modern styling</name>
  <files>editor/src/components/editor/header.tsx</files>
  <action>
Modernize `editor/src/components/editor/header.tsx` with polished styling matching the CapCut/Descript aesthetic:

1. **Header container**: Update from `bg-card` to `bg-[var(--panel-background)]` or equivalent token. Height stays at 52px. Add subtle bottom separator: `border-b border-white/5` (nearly invisible, per seamless design principle). Remove the `z-10` unless needed for layering.

2. **Left section — Logo + File menu + Undo/Redo**:
   - Logo area: Update `bg-primary/20` to `bg-accent-purple-500/15` and use a clean 32x32 container (`h-8 w-8 rounded-lg`).
   - File dropdown: Keep the ghost button trigger. Ensure dropdown content uses `bg-popover` token for dark popover background.
   - Undo/Redo buttons: Use consistent `size-8` icon buttons with `variant="ghost"`. Icons at `size-4` (16px). Remove the extra `text-muted-foreground` className on redo — let the disabled state handle dimming.
   - Tighten the spacing: `gap-1` instead of `gap-2` between logo and file menu.

3. **Center section — Project title**:
   - Keep "Untitled video" centered. Use `text-sm font-medium text-muted-foreground` for a subdued, professional look (not white text).
   - Position with `absolute left-1/2 -translate-x-1/2` (already exists).

4. **Right section — Actions**:
   - Keyboard shortcut button: Keep as ghost icon button, `size-8`.
   - AI Chat toggle: Update to use the new `accent` button variant (purple-tinted from Plan 01) when active, `outline` when inactive. Use `isCopilotVisible` to toggle between variants.
   - Discord link: Simplify to ghost icon button (remove "Join Us" text for cleaner look, keep tooltip).
   - Share button: Use `variant="outline"` with consistent `h-8 px-3 rounded-lg` sizing.
   - Download/Export button: Make this the visual primary action — `variant="default"` (purple fill) with `h-8 px-4 rounded-lg font-medium`. This is the most important CTA.
   - Reduce `gap-2` to `gap-1.5` between buttons for tighter grouping.

5. **Icon consistency**:
   - All toolbar icons: `size-4` (16px) in standard buttons, `size-5` (20px) for primary actions.
   - Use Lucide icons consistently where possible (the header currently mixes @tabler/icons-react for IconShare). Replace `IconShare` with Lucide's `Share2` icon.
   - Standardize: `className="size-4"` pattern (not `className="h-4 w-4"`).

6. **Remove visual clutter**:
   - Remove the ExportModal and ShortcutsModal component declarations from the JSX — they should be rendered conditionally and don't need to always be in the DOM (they already have open/onOpenChange props, so they handle their own visibility). Actually, keep them as-is if they use Radix Dialog which handles mounting internally. Focus on the visual styling.
  </action>
  <verify>
Read header.tsx and confirm: updated background color, consistent icon sizing (size-4/size-5), purple accent on download button, AI Chat uses accent variant, no mixed icon sizing patterns.
Run `pnpm --filter editor build` — no TypeScript errors.
  </verify>
  <done>
Header toolbar has modern, polished styling with: purple accent on primary download action, consistent icon sizes, clean spacing, subdued project title, and professional button hierarchy.
  </done>
</task>

<task type="auto">
  <name>Task 2: Polish timeline toolbar and ruler</name>
  <files>
    editor/src/components/editor/timeline/timeline-toolbar.tsx
    editor/src/components/editor/timeline/timeline-ruler.tsx
  </files>
  <action>
Modernize `timeline-toolbar.tsx` and `timeline-ruler.tsx`:

**timeline-toolbar.tsx:**

1. **Container**: Update from `border-b h-10` to `h-10 bg-[var(--panel-background)]` — no visible border, use background shade. Add subtle `border-b border-white/5` (barely visible).

2. **Left section — Edit tools (split, duplicate, delete, snap)**:
   - Standardize icon sizes: `size-4` (16px) for all toolbar icons.
   - Group related tools visually: Add `bg-white/5 rounded-md p-0.5` wrapper around the tool button group for subtle visual grouping.
   - Use Lucide icons consistently — the file currently mixes Lucide and Tabler. Keep Lucide for standard tools (Scissors, Copy, Trash2, Magnet). These are already Lucide, which is correct.

3. **Center section — Playback controls**:
   - Make playback controls visually prominent: the play/pause button should be slightly larger (`size-9` instead of default) with `bg-white/10 rounded-full hover:bg-white/15` for a circular play button feel.
   - Skip back/forward buttons: `size-7` with `variant="ghost"`.
   - Replace Tabler play/pause icons (IconPlayerPlayFilled, IconPlayerPauseFilled) with Lucide equivalents (Play, Pause) for icon consistency. Use `fill="currentColor"` on the Play icon for filled appearance.
   - Time display: Keep the EditableTimecode + duration display. Ensure `text-xs text-muted-foreground` for the separator and duration. The MM:SS format is already correct (locked decision: mm:ss format).

4. **Right section — Zoom controls**:
   - Keep ZoomIn/ZoomOut buttons + Slider. Standardize icon sizes to `size-4`.
   - Slider: Keep `w-24`. Ensure it uses the design system colors (track: `bg-white/10`, thumb: `bg-foreground`).
   - Group zoom controls: wrap in subtle visual container matching the edit tools group.

5. **Tooltip consistency**: All tooltips should use `delayDuration={300}` (not 500 as currently set). Use a single `<TooltipProvider>` wrapping the entire toolbar (already has two, consolidate to one).

**timeline-ruler.tsx:**

1. Read the file first to understand current implementation.
2. Ensure time labels use mm:ss format (locked decision). If already using mm:ss, leave as-is.
3. Update ruler background to seamlessly blend with timeline area (no visible border between ruler and tracks).
4. Use `text-xs text-muted-foreground` for time labels.
5. Ensure ruler markers use `bg-white/10` for minor ticks and `bg-white/20` for major ticks.
  </action>
  <verify>
Read timeline-toolbar.tsx and confirm: consistent size-4 icons, circular play button, Lucide icons throughout, single TooltipProvider, bg-white/5 tool groups.
Read timeline-ruler.tsx and confirm: mm:ss format, seamless background, text-muted-foreground for labels.
Run `pnpm --filter editor build` — no TypeScript errors.
  </verify>
  <done>
Timeline toolbar has modern playback controls with circular play button, consistent icon sizing, visual tool grouping, and polished zoom controls. Timeline ruler uses mm:ss format with seamless background integration.
  </done>
</task>

</tasks>

<verification>
1. `pnpm --filter editor dev` starts without errors
2. Header shows purple download button as primary CTA
3. Header icons are all consistently sized
4. Timeline toolbar has visually grouped edit tools and prominent play button
5. Time display shows mm:ss format in both toolbar and ruler
6. No visible heavy borders on toolbar areas
</verification>

<success_criteria>
- Header and timeline toolbar both have modern, polished styling
- Icon sizes are consistent (16px standard, 20px prominent)
- Purple accent appears on primary actions
- mm:ss time format used consistently
- No mixed icon library usage within the same toolbar area
</success_criteria>

<output>
After completion, create `.planning/phases/01-editor-polish/01-03-SUMMARY.md`
</output>
